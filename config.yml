# Configuration File
# Sensitive information (API keys, etc.) should be stored in .env file

backend:
  # mongodb_uri: "mongodb://localhost:27017"  # Use MONGODB_URI in .env
  # database_name: "nl_tps"  # Use DATABASE_NAME in .env
  # openai_api_key: "sk-xxx"  # Use OPENAI_API_KEY in .env
  # openai_base_url: "https://api.openai.com/v1"  # Use OPENAI_BASE_URL in .env
  # openai_model_name: "gpt-4"  # Use OPENAI_MODEL_NAME in .env
  # fixed_tenant_id: 1  # Use FIXED_TENANT_ID in .env (development only)

frontend:
  nlTpsApiUrl: "http://localhost:8000/v1"
  membershipApiUrl: ""
  membershipLoginPath: "/v2/auth/login"
  membershipUserPath: "/v2/users/me"
