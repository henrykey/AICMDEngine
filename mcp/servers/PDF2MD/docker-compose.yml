version: '3.8'
services:
  pdf2md:
    build: .
    container_name: pdf2md-mcp
    env_file:
      - .env
    environment:
      PYTHONPATH: /app
    ports:
      - "8030:8030"
    volumes:
      - ./data:/app/data
      - ./config:/app/config
    restart: unless-stopped
